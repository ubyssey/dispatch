language: python
python:
  - "2.7"
  # command to install dependencies
env:
  - DJANGO_VERSION=1.3.1
install: "pip install -r requirements.txt"
  # command to run tests
script: python manage.py test core
script: python manage.py test content
